    // display the workers only basicaly the roles of workers 
  // we need to see the tags like which work is associated here and then be able click on the tags and then see the issues that are associated with the tags
  // so we need to create a schema for this thing and then 
  // we need a interface like thing like in this scenario we will be displayedal the avaiblle workers to assign and herre we need a route for finding those route 


class workersContrller {
    static async getAllWorkers(req: Request, res: Response) {

    }
    static async assignToWorker(req: Request, res: Response) {

    }
    static async getWorkersByTag(req: Request, res: Response) {

    }
    static async getWorkersByIssue(req: Request, res: Response) {

    }
    static async getWorkersByLocation(req: Request, res: Response) {

    }
    static async getWorkersByAvailability(req: Request, res: Response) {

    }
    // an another route to assign route to auto assign the worker using queue system using the round robin methood
    static async autoAssign(req: Request, res: Response) {
        
    }
}